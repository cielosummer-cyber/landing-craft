# 贡献指南 · Contributing

落地工坊最欢迎的贡献是**参考卡**——这个项目的飞轮是「卡库随使用增值」，你跑过的单子、你看到的好网站，都可能冻成下一张卡。

## 贡献一张新参考卡

一张能进库的卡有四件套，缺一件不收：

1. **卡面**：按 `references/cards/_template.md` 的提取协议与模板填写，存 `references/cards/{方向名}.md`。硬要求：
   - 全部令牌可执行（色值/字号/缓动/延迟都是数值，没有「高级感」「流畅」这类词）
   - **禁忌栏必填**——写不出禁忌说明这个方向还没看懂，这是卡库的入门门槛
   - 注明来源（URL / 视频 / 截图）与提取日期；是实地提取（computedStyle 实测）还是目测取色，写清楚
2. **最小 demo**：`references/cards/{方向名}/demo/index.html`——单文件、零依赖、双击直开、一页以内，只演签名交互和排版令牌，不放真实文案图像（卡是语法书不是课文）
3. **PROMPT.md**：demo 目录下一段复刻提示词，让别人（和 agent）能一句话重做这个 demo
4. **差异化说明**：PR 描述里一句话说清这张卡和库里既有卡不是同一个方向换皮（尤其撞底色/撞 accent 的）

新卡进库先挂**基准卡**身份（未验证）：第一次被真实单子使用时验收加倍严格，用过了、坑回填了，状态才改 ✅。这是所有卡都走过的路，不是区别对待。

### 验收装置（卡的 CI）

提交前请自己先跑一遍：

```bash
node scripts/shoot.mjs /绝对路径/references/cards/{方向名}/demo/index.html
```

要求：`pageerror: 0` + 三张截图（首屏/中段/全页）对照 `references/quality-gates.md` 逐项过 + demo 支持 `?shot=1`（URL 带 shot=1 时所有入场动画立即置完成态）。

## 回填修正（同样值钱）

用某张卡跑了真实单子，踩了坑、调了参数？直接把「验证修正」追加进那张卡的卡面（注明日期与场景）。卡库里最值钱的段落全是回填出来的。

## 其他贡献

- **动效片段**：`assets/snippets/` 级别的可复用片段，同样要求 `?shot=1` 兼容 + 零依赖双击直开
- **脚本问题**：`scripts/` 的 bug 直接提 issue，附系统版本与报错全文
- **提取源线索**：发现值得提取的世界级站点/视频，可提 issue 丢进 `references/sources.md` 候选

## 红线（对所有贡献生效）

**学语法不抄课文。** 参考卡只提取设计语法（色板角色、排版机制、动效参数），不得包含来源站点的内容资产（图像、文案、商标、整页截图）。PR 里附上第三方站点的整页截图会被直接打回——提取证据用文字描述色值和参数即可。

## License

提交的贡献默认与本仓库一致，按 MIT License 发布。
