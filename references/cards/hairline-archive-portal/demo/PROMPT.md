# 复刻提示词 · hairline-archive-portal 最小 demo

> 用法：把下面这段贴给 agent，可复刻出本 demo 的语法骨架（令牌 + 签名交互），再按单子主题替换内容。

复刻 `hairline-archive-portal` 参考卡的最小语法演示：只输出一个零依赖、可用 `file://` 双击直开的 HTML，不加载字体、脚本或图像资源，也不放真实品牌、文案、Logo、壁画和文物图；色板严格限定为黑底 `#000000`、白字 `#FFFFFF`、唯一动作色朱红 `#B81010`、非当前索引轨 `rgba(208,206,204,.32)`、1px 分隔线 `rgba(255,255,255,.20)` 和 footer 元信息 `#3E3E3E`；每个场景铺一张**中性灰阶渐变占位图**演示「满幅照片 filter:none 直出」令牌（径向/线性渐变峰值不超过 #262626、不带任何色相——教训：占位只许卡内令牌或中性灰阶），实单换成主体自带暗部的真实选图；全场声明字体栈 `"Helvetica Neue", Helvetica, Arial, sans-serif`，中文主标题 `100px/300/100px`，英文副题 `80px/100/80px`、字距 `5px`、左内缩 `50px`，背景 ghost type 仅声明 `"Playfair Display"`（源站只声明未加载，demo 同样不加载）、`42vh/900/300px`、白色透明度 `.03`，左侧标识用 `writing-mode:vertical-rl`、`20px/300/.2em` 下接 1px×40px 白线；一屏只演一个签名交互：四个 `100vw × 100vh` 场景组成横向主轨，场景间距 `10px`，切换 `300ms`，点击底部四段索引或横向拖动超过 `5px` 阈值后循环切换（首尾克隆实现真无缝 loop，边界阻力 `resistanceRatio .85`、`touchRatio 1`、`touch-action:pan-y`），**不自动播放**；索引轨桌面宽 `75%`、高 `127px`、`right:0`、`bottom:50px`，四段等分，每段高 `90px`、上距 `10px`，非当前为玻璃灰、相邻段 1px 白色 20% 分隔，当前变朱红、`padding:5px 0` 上抬，全部状态过渡 `0.2s ease-in-out`，idle 段 hover 反转白底黑字；当前场景标题和副题执行 `fadeIn 1s ease-out .6s both`，非当前强制隐藏并取消动画；移动端仅采用源站已有令牌：索引轨宽 `220px`、top `100px`，段宽 `200px`，非当前 `translateX(50px)`、当前 `translateX(10px)`，主标题 `40px/300`、副题 `20px/100`；禁用 spotlight、星云 glow、粒子、自动轮播、多彩矿物 UI、宋体/书法标题、圆角卡片、阴影和第二个 accent。
