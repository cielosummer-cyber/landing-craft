# 数字非遗·暗夜流光 参考卡

- 来源：`teamlab.art`（近黑底 + 全幅沉浸影像 + 极薄 UI）、`hkpm.org.hk`（黑底文物聚光摄影 + 金 accent）、`artic.edu`（单 accent 贯穿用法，绯红 #B50938 实测）、`mplus.org.hk`（全幅视频 hero + 巨无衬线标题）、`ihchina.cn`（**对照源：hero 反例、内屏正例**——名录传统色条谱 / 地图数据屏 / 竖栏展览 / 节气顶栏值得学，首屏纹样堆砌是教训）。提取截图存 `refs/digital-heritage-immersive/`（2026-08-22）
- 气质关键词：暗夜、文物聚光、沉浸影像、一个朱砂、呼吸留白
- 适用：非遗/文博/文化数字化/展馆触屏大屏/文化科技品牌首页；「新时代非遗」「数字+科技传承」诉求
- 不适用：快消/母婴/轻快生活方式（黑底压不住）；高信息密度的政务资讯列表页（全幅影像与列表天生冲突）

## 色板（带角色，不临场发明新色）
- BG: `#0E0F0E` — 近黑（teamLab 实测值，带一丝灰绿，不是纯黑——纯黑在大屏上显脏）
- INK: `#F2EFE9` — 暖白（展签宣纸感，禁纯白长文）
- ACCENT: `#C8391F` — 朱砂，唯一强调色/动作色（从 AIC 绯红 #B50938 向暖调偏移；编号、关键词、CTA 描边、当前态）
- MUTED: `rgba(242,239,233,0.55)`
- HAIRLINE: `rgba(242,239,233,0.14)`
- PANEL: `#B3A087` / 深色阶 `#9E8C73` — 米色数据卡（ihchina 地图屏实测）：数据面板、统计卡专用底，配墨字不用白字
- 门类辅助色族（传统矿物色，**仅**用于门类标识/竖栏底色，从 ihchina 名录屏取色估算：黛蓝 `#3A4457`、松绿 `#3E5648`、赭石 `#6B4A3F`、米白 `#D8D2B8`）——规则：降饱和使用、彼此不抢、朱砂仍是唯一动作色；首次使用前用取色器复核

## 字体配对
- Display 中文: Noto Serif SC 900（fallback Songti SC/STSong）— 黑底宋体大标题 = 碑刻感，clamp(72px, 12vw, 180px)
- 竖排栏目题: 同宋体 700 + `writing-mode: vertical-rl`（ihchina 展览屏/名录屏的成熟用法）
- Label/Mono: JetBrains Mono 300 uppercase + tracking 0.2em — 展签、编号、参数行、数据
- 正文: Noto Sans SC 400，1.9 行高——黑底正文用无衬线，宋体长文糊

## 签名交互（每单选一个）
- 最小演示：`demo/index.html`（零依赖双击直开，两个签名交互 + 排版令牌全参数；复刻提示词 `demo/PROMPT.md`）
1. **文物聚光 spotlight**（首选）：展品图整体压暗至 `brightness(0.35)`，radial-gradient 光斑（边缘 feather 60%）跟随指针/触摸点，rAF + LERP 平滑；光斑内恢复 `brightness(1)`——观众用手「点亮」文物。参数：`SPOT_R=320px`（75 寸触屏 480px）、`LERP=0.08`、`DIM=0.35`。待机变体：光斑沿利萨如轨迹自游走，或换 `assets/snippets/bg-flow-field` 粒子流
2. **竖栏手风琴**（ihchina 展览屏模式，触屏天然适配）：5-7 根竖栏各占门类/主题，栏底压暗的文物摄影 + 门类矿物色底 + 竖排宋体栏题，触摸展开该栏至 60% 宽、其余栏收缩，展开态显展签参数行。参数：栏数 ≤7、展开动画 600ms cubic-bezier(.16,1,.3,1)、未展开栏图 `brightness(0.45)`

## 排版节奏
- 全幅出血影像打底，UI 退到 hairline 和 mono 小字（学 teamLab：作品即界面）
- 内容块 max 1200px **偏左**对齐（反对称）；section 分隔 = hairline + 大字编号「壹/贰/叁」或 mono `01/02/03`
- 展签式参数行沿用新中式卡的 hairline 行式表（hover 朱砂 6% 底）
- M+ 式巨型短句标题可用在 hero：一行不超过 8 字，两行以内
- **黑底巨标题主动断行，不依赖自动折行**：8 字标题按 4+4 拆两行（上行 INK、下行 ACCENT），各加 `white-space:nowrap`——首单实测自动折行必孤字（「谱」字单挂一行，2026-08-22 内部验证单）

## ihchina.cn 内屏借鉴清单（深滚后的重新评估，2026-08-22）
- **节气/农历时间角标**：顶栏「农历七月初十 · 立秋」+ 节气小印——非遗与节气强内容关联，待机屏/顶栏可继承（mono 化重排，别用他们的楷体）
- **名录传统色条谱**：整屏矿物色竖条 + 竖排白字标题，色彩本身即内容——可做「门类色导览」屏
- **地图 + 米色数据卡**：暗底暖棕摄影上叠描边地图 + `#B3A087` 数据面板（大数字 + 小字门类名 + 审图号式来源标注）——知识图谱「地图态」的直接参照
- **竖栏展览手风琴**：见签名交互 2
- **中国结式分页指示器**：符号被功能化（当前项填朱砂）——传统符号的正确用法示范
- **内容架构**：名录 → 国际名录 → 动态 → 地图 → 研培 → 展览影音，一屏一主题的 fullpage 节奏适合大屏

## 图像处理
- 文物/非遗图一律 chiaroscuro：黑底拍摄或后期压暗，单一光源感；禁白底证件照式摆拍
- 全幅出血，不加圆角不加边框；hover/触摸 `scale(1.03)` 图内缩放
- 生成图提示词落点：「single artifact on pure black background, single dramatic spotlight, museum chiaroscuro, dark moody」（Ark/codex 生图均适用）

## 禁忌（这个方向做了就破功的事）
- **禁传统纹样无层次铺满**——ihchina hero 的老气就来自祥云/刺绣/回纹等亮度互相打架地堆满屏。纹样只允许两种存在方式：① ≤8% 透明度暗纹压在一个矿物色底上（ihchina 展览屏墨黑栏的正面用法）；② 作为「一件展品的内容」（某件织绣的局部特写）。永远不当壁纸
- **禁符号堆砌**：印章 + 农历条 + 毛笔字 Logo + 云纹边框同时出现 = 政务老干部风。传统符号必须**功能化**才能留（中国结做分页器是正面案例），纯装饰的一个不留
- **禁居中对称的奖状式构图**——一律偏轴、留出一侧呼吸
- **禁多色抢戏**：门类矿物色族只当底色/标识，金、红、石青不得同时充当强调色；accent 只有朱砂一个，金色只允许出现在文物照片本身的材质里
- **禁黑底暗金小字长文**——可读性崩；正文必须暖白
